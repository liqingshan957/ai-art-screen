# 广州美术馆 · 敦煌AIGC艺术展 — 投屏系统使用说明

## 系统概述

一套用于广州美术馆敦煌AI艺术展的现场运营工具，包含大屏作品展示、手机端作品页分享、后台管理三大模块。

**技术栈**：Node.js + Express + Socket.IO（实时推送）

---

## 核心地址

| 用途 | 地址 |
|------|------|
| 后台管理（上传作品） | `http://localhost:3000/admin` |
| 大屏展示页 | `http://localhost:3000/display` |
| 手机作品页（公网） | `https://gzart-o8114r7d.pagefire.openhkt.com/works/{作品ID}.html` |

---

## 功能模块

### 1. 作品上传（Admin 后台）

- **单张上传**：填入孩子名字 + 日期（8位如 `20260721`），选择图片上传
- **批量上传**：粘贴多个名字（一行一个） + 对应日期，一次性多张图片
- 上传后自动生成作品页 + 部署到公网

### 2. 大屏展示（Display）

- 实时接收后台推送的作品，卡片式排列展示
- 支持背景图切换、视频播放
- 通过 Socket.IO 实时同步，无需手动刷新

### 3. 手机作品页（微信分享）

- 每个作品自动生成独立 H5 页面
- 页面内容：品牌 Logo → 作品大图 → 孩子名字 + 日期 → 长按保存引导 → 二楼课程引流卡片
- 通过 PageFire 部署到公网 HTTPS，微信可直接打开
- 分享时自动带作品缩略图（OpenGraph）

### 4. 飞书同步

- 自动从飞书多维表格拉取「姓名 + 作品 + 日期」数据
- 每 30 秒轮询一次，发现新记录自动创建作品

### 5. PageFire 自动部署

- 作品上传后约 15 秒自动部署到公网
- Admin 后台顶部显示同步状态（✅ 已同步 / ⏳ 同步中）
- 点击「📋 作品页」一键复制公网链接

---

## 启动方式

双击 **`启动投屏系统.bat`**（一键启动全部服务）。

> ⚠️ 这是**唯一正确的启动方式**！它自动依次启动3个服务：
> 1. **Rembg 抠图服务**（端口 7000）→ 自动抠图必备
> 2. **投屏主服务**（端口 3000）→ Express + Socket.IO
> 3. **收件箱监听** → 监控B电脑新图片
>
> 启动后后台会有3个最小化窗口，**不要关闭它们**。

### 单独启动（调试用）

| 用途 | 命令 |
|------|------|
| 只启动投屏服务 | `start-screen.bat` |
| 只启动抠图服务 | `start-rembg.bat` |
| 只启动收件箱监听 | `start-inbox.bat` |

> **不要**用旧的 `node server.js` 或单独双击某个 .bat，否则自动抠图功能会不可用！

### 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 浏览器弹出 Gradio 网页 | 旧版 rembg 的 UI 界面 | ✅ **已修复** — 加了 `--no-ui` 参数 |
| 自动抠图失败/超时 | rembg 没启动（只启动了主服务） | ✅ **已修复** — 一键启动会自动启动 rembg |
| 提示"模型加载失败" | `u2net.onnx` 模型文件损坏 | ✅ **已修复** — 已替换为正确的 167MB 模型文件 |

---
## 日常运营流程

1. **现场收集作品** → 通过 Admin 后台上传
2. **大屏实时展示** → 打开 Display 页（Chrome 全屏）
3. **分享给家长** → Admin 后台点「📋 复制链接」→ 微信发给家长
4. **家长打开链接** → 看到孩子作品 → 点击图片/长按保存 → 看到二楼课程引导

---

## 文件结构

```
投屏系统/
├── server.js              # 主服务（API + Socket.IO + 作品页生成）
├── feishu_sync.js         # 飞书数据同步
├── 启动投屏系统.bat        # 一键启动脚本
├── public/
│   ├── admin.html         # 后台管理页
│   ├── display.html       # 大屏展示页
│   ├── logo.png           # 品牌 Logo
│   ├── uploads/artworks/  # 上传的作品原图
│   └── works/             # 生成的静态作品页（本地）
├── deploy-pagefire/       # PageFire 部署目录
│   ├── works/             # 作品页（公网版）
│   └── artworks/          # 压缩后的作品图
└── data/
    ├── artworks.json      # 作品数据
    └── feishu_synced.json # 飞书同步记录
```

