# 广州美术馆 · 敦煌AIGC艺术展 — 投屏系统使用说明

## 系统概述

一套用于广州美术馆敦煌AI艺术展的现场运营工具，包含三大模块：
1. **大屏作品展示**（电视/投影）— 浮动卡片 + spotlight 特写
2. **自动抠图上屏**（收件箱监听）— B电脑来图 → 自动抠图 → 上屏
3. **后台管理**（上传/统计/视频配置）

**技术栈**：Node.js + Express + Socket.IO + Sharp（裁剪）+ Rembg（抠图）

---

## 核心地址

| 用途 | 地址 |
|------|------|
| 后台管理（上传作品、配置视频等） | `http://localhost:3000/admin` |
| 大屏展示页 | `http://localhost:3000/display` |
| 手机作品页（公网） | `https://gzart-o8114r7d.pagefire.openhkt.com/works/{作品ID}.html` |

---

## 功能模块

### 1. 自动抠图上屏（核心流程）

B 电脑上的学生作品图片 → 自动下载 → 裁剪去边距 → 抠图 → 大屏特写展示。

**完整链路：**

```
B电脑 (172.16.29.64:8765)
  → 收件箱监听每2秒轮询发现新文件
  → 下载到 watch-folder/
  → POST http://localhost:3000/api/auto-matting
  → 服务器处理：
      ① 保存原图到 originals/{id}.{ext}
      ② sharp 裁剪去边距（设计卡 102×152mm 规格）
         上裁8mm / 左右8mm / 下32mm → 可见区域 86×112mm
         保存裁剪版到 originals/{id}_c.{ext}
      ③ rembg 抠图（扣除背景）
      ④ 保存抠图版到 artworks/{id}.png
      ⑤ 创建作品数据 → 存入 artworks.json
      ⑥ socket.io 推送到大屏
  → 收件箱监听收到成功响应 → 文件移入 printed-folder/
  → 大屏收到新作品 → 启动 spotlight 特写流程
```

### 2. 大屏展示（Display）

**浮动画廊：**
- 始终保持 6 张浮动卡片在屏幕上
- 每张卡片随机漂移、旋转、碰撞反弹
- 每张卡片寿命 10~20 秒 → 淡出 → 补新卡

**特写流程（spotlight）：**
```
收到新作品（有 originalUrl）
  → 无视频排队 / 视频结束后 → 启动特写
  → ① 入场爆发（冲击波 + 粒子）
  → ② 展示裁剪后的原图（居中，60%屏高）
  → ③ 旋转光效 + 粒子持续（5.5秒）
  → ④ 缩小到随机卡片位置（1.3秒）
  → ⑤ 闪光 crossfade → 原图变抠图版
  → ⑥ 在缩小位置创建浮动卡片 → 开始漂动
```

**视频插播：**
- 定时全屏播放展示视频
- 视频播放间隔和重复次数可在 Admin 后台设置
- 视频播放期间新作品会排队等待，视频结束后依次呈现

### 3. 作品上传（Admin 后台）

| 方式 | 说明 |
|------|------|
| 单张上传 | 填入名字 + 日期，选择图片上传 |
| 批量上传 | 粘贴多个名字（一行一个），一次性多张图片 |
| 自动抠图上屏 | 由收件箱监听自动完成（见上方） |

**两种上传路径的区别：**

| | 自动抠图上传 | Admin 手动上传 |
|------|------------|--------------|
| 入口 | 收件箱监听 → /api/auto-matting | Admin → /api/artworks/upload |
| 是否抠图 | ✅ rembg 扣除背景 | ❌ 不抠图 |
| 是否裁剪 | ✅ sharp 去边距 | ❌ 不裁剪 |
| 特写效果 | ✅ 有（先展示原图 → 过渡到抠图版） | ❌ 无（直接变浮动卡片） |
| 保存原图 | ✅ originals/{id}.{ext} | ❌ 不保存 |

### 4. 手机作品页（微信分享）

- 每个作品自动生成独立 H5 页面
- 页面内容：品牌 Logo → 作品大图 → 孩子名字 + 日期 → 长按保存引导
- 通过 PageFire 部署到公网 HTTPS，微信可直接打开

### 5. 飞书同步

- 自动从飞书多维表格拉取「姓名 + 作品 + 日期」数据
- 每 30 秒轮询一次，发现新记录自动创建作品

### 6. PageFire 自动部署

- 作品上传后约 15 秒自动部署到公网
- Admin 后台顶部显示同步状态（✅ 已同步 / ⏳ 同步中）

---

## 启动方式

双击 **`启动投屏系统.bat`**（一键启动全部服务）。

> ⚠️ 这是**唯一正确的启动方式**！它自动依次启动 3 个服务：
> 1. **Rembg 抠图服务**（端口 7000）→ 自动抠图必备
> 2. **投屏主服务**（端口 3000）→ Express + Socket.IO + Sharp
> 3. **收件箱监听** → 监控 B 电脑新图片
>
> 启动后会有 3 个最小化窗口，**不要关闭它们**。

### 单独启动（调试用）

| 用途 | 命令 |
|------|------|
| 只启动投屏服务 | `start-screen.bat` |
| 只启动抠图服务 | `start-rembg.bat` |
| 只启动收件箱监听 | `start-inbox.bat` |

---

## 配置说明

### 视频配置（Admin 后台可调）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 播放间隔(interval) | 300 秒 | 每隔多少秒播放一次视频 |
| 重复次数(repeat) | 2 次 | 每次播放循环次数 |

配置存储在 `data/videos_config.json`

### 大屏参数（需改代码）

在 `public/js/display.js` 中：

| 常量 | 默认值 | 说明 |
|------|--------|------|
| FIXED_CARDS | 6 | 同时显示的浮动卡片数 |
| MIN_LIFETIME | 10000 | 卡片最短寿命(ms) |
| MAX_LIFETIME | 20000 | 卡片最长寿命(ms) |
| originalDuration | 5500 | 特写展示时长(ms) |

### 收件箱监听配置

在 `D:\桌面\开发项目\收件箱监听\config.json`：

| 字段 | 默认值 | 说明 |
|------|--------|------|
| remotes | [{name, url}] | B/C 电脑的 HTTP 地址 |
| screenSystemUrl | http://localhost:3000 | 投屏系统地址 |
| pollInterval | 2000 | 轮询间隔(ms) |

---

## 常见问题

### 上传失败

| 现象 | 原因 | 解决 |
|------|------|------|
| 返回 "xxx is not defined" | git checkout 恢复了旧版本，缺少常量 | 从 GitHub 拉取完整版 `git pull` |
| 返回 "Rembg timeout" | rembg 服务没启动 | 双击 `start-rembg.bat` |
| 文件留在 watch-folder 不动 | 收件箱监听没有重试机制 | 重启监听 + 清空 watch-folder（让 B 电脑重新下载）|
| 上传后大屏没反应 | 服务器端口 3000 未启动 | 检查 `http://localhost:3000` 能否打开 |

### 大屏展示

| 现象 | 原因 | 解决 |
|------|------|------|
| 新作品不显示 | 有视频配置 → 作品排队等视频结束 | 等视频播完，或清空视频列表 |
| 卡片消失太快 | MIN/MAX_LIFETIME 太短 | 改 `display.js` 14-15 行 |
| 视频间隔太短/太长 | interval 值不合适 | Admin → 视频管理 → 调整 |
| 特写位置偏了 | CSS 定位问题 | 检查 `display.css` 中 `#spotlight-image-wrap` |

### 收件箱监听

| 现象 | 原因 | 解决 |
|------|------|------|
| 连不上 B 电脑 | B 电脑 HTTP 服务没跑 | B 电脑运行 `python -m http.server 8765` |
| 连不上 B 电脑 | 防火墙阻拦 | Windows 防火墙放行 Python |
| 连不上 B 电脑 | IP 地址不对 | 检查 `config.json` 中的地址 |

---

## 文件结构

```
投屏系统/
├── server.js                    # 主服务（API + Socket + 抠图 + 裁剪）
├── feishu_sync.js               # 飞书数据同步
├── 启动投屏系统.bat              # 一键启动脚本
├── start-*.bat                  # 各服务独立启动脚本
├── 投屏系统使用说明.md            # 本文档
├── public/
│   ├── display.html             # 大屏展示页
│   ├── admin.html               # 后台管理页
│   ├── dashboard.html           # 数据看板
│   ├── css/display.css          # 大屏样式
│   ├── js/
│   │   ├── display.js           # 大屏逻辑（特写、卡片、视频）
│   │   └── admin.js             # 后台逻辑
│   └── uploads/
│       ├── artworks/            # 抠图版作品（已裁剪）
│       ├── originals/           # 原图 + 裁剪版原图（{id}_c.{ext}）
│       ├── background/          # 背景图
│       └── videos/              # 插播视频
├── deploy-pagefire/             # PageFire 公网部署产物
├── data/                        # 运行时数据（动态变化）
│   ├── artworks.json            # 作品数据
│   ├── videos.json              # 视频列表
│   ├── videos_config.json       # 视频播放配置
│   └── analytics.json           # 访问统计
├── .ai-assist/                  # AI 开发管理体系文档
│   ├── MANIFEST.md              # 项目入口
│   ├── ARCHITECTURE.md          # 系统架构
│   ├── FILE_REFERENCE.md        # 文件职责与关键函数
│   ├── DEVELOPMENT_GUIDE.md     # 开发规范
│   ├── CONFIGURATION.md         # 配置汇总
│   ├── TROUBLESHOOTING.md       # 常见问题
│   └── CHANGE_LOG.md            # 修改记录
├── .workbuddy/                  # 项目记忆（历史记录）
└── test-rembg/                  # 抠图模型缓存
```

### 收件箱监听（独立项目）

位于 `D:\桌面\开发项目\收件箱监听\`

```
收件箱监听/
├── inbox-watcher.js             # 主程序
├── config.json                  # 配置（远程地址、轮询间隔）
├── watch-folder/                # 下载的图片（上传成功 → 移走）
├── printed-folder/              # 已上传成功的图片
└── .ai-assist/                  # AI 开发文档
```

---

## 开发参考

详细的开发文档见 `.ai-assist/` 目录：

| 文档 | 内容 |
|------|------|
| `ARCHITECTURE.md` | 系统架构、数据流 |
| `FILE_REFERENCE.md` | 文件职责、关键函数、常量、配置 |
| `DEVELOPMENT_GUIDE.md` | 安全修改流程、常犯错误 |
| `CONFIGURATION.md` | 所有配置项汇总 |
| `TROUBLESHOOTING.md` | 常见问题与修复步骤 |
| `CHANGE_LOG.md` | 历次修改记录 |

---

## 日常运营流程

1. **现场收集作品** → 通过 Admin 后台上传（或 B 电脑共享目录自动上传）
2. **大屏实时展示** → 打开 Display 页（Chrome 全屏）
3. **分享给家长** → Admin 后台点「📋 复制链接」→ 微信发给家长
4. **家长打开链接** → 看到孩子作品 → 长按保存 → 看到二楼课程引导